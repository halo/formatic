<%
  record = House.new(floors: 'many')
  record.errors.add(:floors, :invalid)
%>
<%= form_for(record, url: '#') do |f| %>
  <%= render Formatic::Textarea.new(f:, attribute_name: :floors) %>
<% end %>
